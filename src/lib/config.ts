/**
 * Centralized product config. Keeping brand strings in one place means the
 * whole app can be re-skinned by editing this file alone.
 */
export const APP_NAME = "Scout";
export const APP_TAGLINE = "A research agent that knows when to delegate.";
export const APP_DESCRIPTION =
  "Scout is a research assistant: a main agent that can dispatch focused sub-agents to investigate parts of a question in parallel.";

/** Default model id (resolved against the provider registry as `openai:gpt-4.1`). */
export const DEFAULT_MODEL = "gpt-4.1";
